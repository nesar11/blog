// styles/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // Your custom theme options here
  // For example, you can define custom colors, typography, etc.
});

export default theme;
